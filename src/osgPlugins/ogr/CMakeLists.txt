SET(TARGET_SRC ReaderWriterOGR.cpp)

SET(TARGET_EXTERNAL_LIBRARIES GDAL::GDAL)
OSG_ADD_PACKAGE_DEPENDENCY(GDAL)

IF(CMAKE_COMPILER_IS_GNUCXX)
   STRING(REGEX REPLACE "-Wshadow" "" CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")
ENDIF()

#### end var setup  ###
SETUP_PLUGIN(ogr)
