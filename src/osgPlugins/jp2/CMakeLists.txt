SET(TARGET_SRC ReaderWriterJP2.cpp )

SET(TARGET_EXTERNAL_LIBRARIES Jasper::Jasper)
OSG_ADD_PACKAGE_DEPENDENCY(Jasper)

# libjasper.a references libjpeg when Jasper is built with its JPEG codec
# enabled.
IF(JPEG_FOUND)
    SET(TARGET_EXTERNAL_LIBRARIES ${TARGET_EXTERNAL_LIBRARIES} JPEG::JPEG)
    OSG_ADD_PACKAGE_DEPENDENCY(JPEG)
ENDIF()

IF(WIN32)
   ADD_DEFINITIONS(-DJAS_WIN_MSVC_BUILD)
ENDIF()


#### end var setup  ###
SETUP_PLUGIN(jp2)
